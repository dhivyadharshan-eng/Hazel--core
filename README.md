# Orion Dashboard

Real-time analytics dashboard. In active development.

## Status
- Auth refactor: **in progress** (auth.service.js)
- Live streaming charts: **not started**
- Demo due: **this Friday**

## Notes for next session
1. Finish auth refactor (`auth.service.js` token rotation)
2. Run the test suite (`npm test`)
3. Wire `/api/streaming` WebSocket for `LiveChart.js`
